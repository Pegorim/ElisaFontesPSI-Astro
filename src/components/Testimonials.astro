---
const testimonials = [
  {
    text: "Sou paciente da Elisa há mais de dois anos, e as consultas foram sempre pontuais e com ótimos resultados",
    author: "Jussara",
    position: "left"
  },
  {
    text: "Profissional muito competente, pontual e tem feito um trabalho incrível com o qual já senti muita diferença na minha vida",
    author: "Lívia",
    position: "right"
  },
  {
    text: "Sensacional, me ajuda demais em todas as questões que preciso e me mostra muita clareza e confiança",
    author: "Rafael",
    position: "left"
  }
];
---

<section class="py-12 bg-primary-bg px-6 md:px-12 lg:px-20">
  <div class="max-w-4xl mx-auto space-y-8">
    {testimonials.map((t) => (
      <div class:list={[
        "flex items-start gap-4",
        t.position === "right" ? "flex-row-reverse" : ""
      ]}>
        <!-- Avatar -->
        <div class="flex-shrink-0 w-10 h-10 rounded-full bg-card-bg overflow-hidden">
          <img
            src={`/images/testimonial-${t.author.toLowerCase()}.jpg`}
            alt={t.author}
            class="w-full h-full object-cover"
            onerror="this.style.display='none'; this.parentElement.innerHTML=this.parentElement.innerHTML + '<span class=\"flex items-center justify-center w-full h-full text-text-main font-bold\">' + this.alt.charAt(0) + '</span>'"
          />
        </div>

        <!-- Chat Bubble -->
        <div class="bg-card-bg rounded-2xl px-6 py-4 max-w-xl relative">
          <p class="text-text-main">
            "{t.text}" <span class="font-medium ml-2">{t.author}</span>
          </p>
        </div>
      </div>
    ))}
  </div>
</section>

<!-- Divider -->
<hr class="border-t border-stone-300 mx-6 md:mx-12 lg:mx-20" />
