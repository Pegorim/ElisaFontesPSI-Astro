---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Hero from '../components/Hero.astro';
import ServiceCards from '../components/ServiceCards.astro';
import Testimonials from '../components/Testimonials.astro';
import BlogGrid from '../components/BlogGrid.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="Terapia com Elisa | Psicóloga Clínica">
	<Header />
	
	<main>
		<Hero />
		<ServiceCards />
		
        <!-- Quote Section -->
        <section class="py-20 bg-card-bg/30 text-center px-4">
            <div class="max-w-3xl mx-auto">
                <blockquote class="font-heading text-2xl md:text-3xl italic text-text-main leading-relaxed">
                    "O autoconhecimento é a chave que liberta você de padrões antigos e abre portas para uma vida mais autêntica."
                </blockquote>
            </div>
        </section>

		<Testimonials />
		
        <!-- About Preview / CTA Strip -->
        <section class="py-16 bg-accent text-white relative overflow-hidden">
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-white/10 to-transparent opacity-30"></div>
            <div class="max-w-4xl mx-auto px-4 text-center relative z-10">
                <h2 class="font-heading text-3xl mb-6">Comece sua transformação hoje</h2>
                <p class="text-white/80 mb-8 max-w-2xl mx-auto text-lg">
                    Não espere o momento "perfeito" para cuidar de você. O primeiro passo é o mais importante.
                </p>
                <a href="#contato" class="inline-block bg-white text-accent px-10 py-4 rounded-full font-bold hover:bg-stone-100 transition-colors shadow-lg">
                    Agendar Sessão
                </a>
            </div>
        </section>

		<BlogGrid limit={3} />
	</main>

	<Footer />
</Layout>
